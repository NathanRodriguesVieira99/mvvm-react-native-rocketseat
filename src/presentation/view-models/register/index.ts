import { AuthFormHeader } from '../../components/auth-form-header';
import { KeyboardContainer } from '../../templates/keyboard-container';

export const Register = {
  KeyboardContainer: KeyboardContainer,
  Header: AuthFormHeader,
};
